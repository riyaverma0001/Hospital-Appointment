<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Bookings</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-50 via-white to-gray-100 font-sans text-gray-800">

  <div class="min-h-screen flex flex-col items-center justify-center p-4">
    <div class="w-full max-w-4xl bg-white rounded-2xl shadow-xl border-t-4 border-blue-500">
      <h2 class="text-3xl font-bold text-center text-blue-600 py-6">All Bookings</h2>

      <!-- Check if bookings array is empty -->
      <% if (bookings.length === 0) { %>
        <p class="text-center text-gray-600 text-lg py-8">No bookings available.</p>
      <% } else { %>
        <ul class="divide-y divide-gray-200">
          <% bookings.forEach((booking) => { %>
            <li class="p-6 hover:bg-gray-50 transition duration-200">
              <div class="flex justify-between items-center">
                <div>
                  <p class="text-lg font-semibold text-gray-800">
                    <strong>Date:</strong> <%= booking.date %>
                  </p>
                  <p class="text-md text-gray-600 mt-1">
                    <strong>Time Slot:</strong> <%= booking.timeSlot %>
                  </p>
                </div>
                <div>
                  <span 
                    class="px-3 py-1 text-sm font-medium rounded-lg 
                    <%= booking.status === 'booked' ? 'bg-yellow-100 text-yellow-700' : 'bg-green-100 text-green-700' %>">
                    <%= booking.status %>
                  </span>
                </div>
              </div>
            </li>
          <% }); %>
        </ul>
      <% } %>
    </div>
  </div>

</body>
</html>
